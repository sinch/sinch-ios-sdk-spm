// Minimal stub to satisfy SwiftPM target; the target carries linkerSettings.

// Swift Package Manager requires at least one source file in any non-binary target. 
// Without it, the target that carries linkerSettings fails as “empty target”.